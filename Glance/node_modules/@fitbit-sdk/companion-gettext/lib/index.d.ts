export interface MessageTable {
    [message: string]: string;
}
export interface LanguageTable {
    [tag: string]: MessageTable;
}
export default function gettextFactory(langTable: LanguageTable, currentLocale?: string, fallbackLocale?: string): (msgid: unknown) => string;
