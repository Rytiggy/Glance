"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var fs = __importStar(require("fs"));
var pngjs_1 = require("pngjs");
var _1 = require(".");
function checkImage(filename, rle, outputFormat) {
    if (outputFormat === void 0) { outputFormat = _1.TXIOutputFormat.RGBA8888; }
    var pngData = fs.readFileSync(filename);
    var uncompressed = pngjs_1.PNG.sync.read(pngData);
    var inputImage = {
        data: new Uint8ClampedArray(uncompressed.data),
        width: uncompressed.width,
        height: uncompressed.height,
    };
    _1.encode(inputImage, { rle: rle, outputFormat: outputFormat });
}
checkImage(process.argv[2], 'auto');
//# sourceMappingURL=profile.js.map