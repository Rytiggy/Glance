/// <reference types="node" />
import * as stream from 'stream';
export declare class ParseJSON extends stream.Transform {
    constructor();
    _transform(chunk: Buffer, encoding: string, callback: (err?: Error) => void): void;
}
export declare class StringifyJSON extends stream.Transform {
    whitespace: string | number | undefined;
    constructor(whitespace?: string | number);
    _transform(chunk: object, encoding: string, callback: (err?: Error) => void): void;
}
