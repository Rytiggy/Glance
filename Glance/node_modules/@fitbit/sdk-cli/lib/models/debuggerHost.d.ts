import { Host } from '@fitbit/fdb-host';
import { FDBTypes } from '@fitbit/fdb-protocol';
export interface HostDescriptor {
    id: string;
    displayName: string;
    capabilities: FDBTypes.ApplicationHostCapabilities;
}
export declare function createDebuggerHost(hostDescriptor: HostDescriptor, handleLog: (msg: string) => void): Promise<{
    closePromise: Promise<void>;
    host: Host;
    close: () => void;
}>;
//# sourceMappingURL=debuggerHost.d.ts.map