"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const fs_extra_1 = require("fs-extra");
const path_1 = tslib_1.__importDefault(require("path"));
const chalk_1 = tslib_1.__importDefault(require("chalk"));
function checkForUpdate(updateNotifier) {
    const packageJSON = fs_extra_1.readJSONSync(path_1.default.join(__dirname, '../package.json'));
    const { update } = updateNotifier({ pkg: packageJSON });
    if (!update)
        return;
    if (update.type === 'patch') {
        const message = `${update.name} update available ${update.current} → ${update.latest}`;
        console.log(chalk_1.default.red(message));
    }
    else {
        const message = "You're targeting an older version of the Fitbit SDK. Consider updating to access new features.";
        console.log(chalk_1.default.keyword('orange')(message));
    }
}
exports.default = checkForUpdate;
