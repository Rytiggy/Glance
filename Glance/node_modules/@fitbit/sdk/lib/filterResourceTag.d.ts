/// <reference types="node" />
import { Transform, TransformCallback } from 'stream';
import Vinyl from 'vinyl';
declare class FilterResourceTag extends Transform {
    private resourceFilterTag;
    private files;
    constructor(resourceFilterTag: string);
    _transform(file: Vinyl, _: any, callback: TransformCallback): void;
    _flush(callback: TransformCallback): void;
}
export default function filterResourceTag(tagName: string): FilterResourceTag;
export {};
