"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const path_1 = tslib_1.__importDefault(require("path"));
function forbidAbsoluteImports() {
    return {
        name: 'forbidAbsoluteImports',
        resolveId(importee, importer) {
            if (!importer)
                return;
            if (importer.startsWith('\0'))
                return;
            if (path_1.default.isAbsolute(importee)) {
                throw new Error(`${importee} is imported by ${importer}, but absolute imports are disallowed`);
            }
        },
    };
}
exports.default = forbidAbsoluteImports;
