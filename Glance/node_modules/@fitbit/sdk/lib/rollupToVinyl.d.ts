/// <reference types="node" />
import { Readable } from 'stream';
import * as rollup from 'rollup';
export default function rollupToVinyl(outputPath: string, inputOptions: rollup.RollupFileOptions, outputOptions: rollup.OutputOptions): Readable;
