<svg id="glance">
	<svg id="singleBG">
		<svg class="row top">
			<use href="#bloodSugarDisplay" class="bloodSugarDisplay"/>
		</svg>
		<svg class="row bottom">
			<use href="#stats" class="stats"/>
			<svg class="right width-50">
				<use href="#graph" class="graph"/>
			</svg>
		</svg>
	</svg>
	<svg id="dualBG" display="none">
		<svg class="row top">
			<use href="#stats" class="stats"/>
			<use href="#graph" class="graph center width-30"/>
			<use href="#bloodSugarDisplay" class="bloodSugarDisplay"/>
		</svg>
		<svg class="row bottom">
			<use href="#stats" class="stats"/>
			<use href="#graph" class="graph center width-30"/>
			<use href="#bloodSugarDisplay" class="bloodSugarDisplay"/>
		</svg>
	</svg>
</svg>
